// Utilidades y helpers compartidos
export { formatDate, formatPrice, capitalize, generateId } from '@/shared/lib/utils';
export { isValidEmail, isValidPassword, isValidPhone, isNotEmpty } from '@/shared/lib/validators';
export { formatFullName, formatPhoneNumber, formatDuration, truncateText } from '@/shared/lib/formatters'; 