// Cliente HTTP genérico compartido
export { HttpClient } from './HttpClient';
export type { 
  ApiResponse, 
  PaginatedResponse, 
  ApiError, 
  RequestConfig, 
  HttpClientConfig 
} from '@/shared/api/types'; 