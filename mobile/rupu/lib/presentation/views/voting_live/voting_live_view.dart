import 'package:flutter/material.dart';

class VotingLiveView extends StatelessWidget {
  static const name = 'voting-live';

  const VotingLiveView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Voting Live View (Placeholder)')),
    );
  }
}
