name: Test Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Test step
        run: |
          echo "✅ Workflow funcionando correctamente!"
          echo "Cambio detectado en: ${{ github.event.head_commit.message }}"
          ls -la .github/workflows/
